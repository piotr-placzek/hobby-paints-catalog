'use strict';

/**
 * @param {cheerio} cheerio cheerio object with loaded html
 * @returns {Scale75Paint[]}
 */
function scScrapingStrategy(cheerio) {
    return [{}];
}

module.exports = scScrapingStrategy;
